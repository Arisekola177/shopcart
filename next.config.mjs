/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['img.freepik.com',"m.media-amazon.com", "images.pexels.com", "lh3.googleusercontent.co", 'firebasestorage.googleapis.com'], 
      
    },
};

export default nextConfig;
